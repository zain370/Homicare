import 'package:flutter/cupertino.dart';

class InProgressAdmin extends StatelessWidget {
  const InProgressAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
