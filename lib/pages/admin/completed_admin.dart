import 'package:flutter/cupertino.dart';

class CompletedAdmin extends StatelessWidget {
  const CompletedAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
